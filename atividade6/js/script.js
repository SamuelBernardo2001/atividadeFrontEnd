document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('sorteio');
    const botaoSorteio = document.getElementById('botao-sorteio'); 
    const resultado = document.getElementById('resultado');
});